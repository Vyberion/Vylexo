export declare class Cooldown {
    Command: typeof Command;
    constructor();
}
declare class Command {
    private guildId;
    private commandName;
    constructor(guildId: string, commandName: string);
    add(userId: string, time: number): void;
    remove(userId: string): void;
    has(userId: string): false | {
        msleft: number;
        secondsleft: number;
        minutesleft: number;
        hoursleft: number;
        daysleft: number;
    };
}
export {};
